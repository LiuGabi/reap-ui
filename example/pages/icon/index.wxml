<i-grid>
    <view class="icon-item" wx:for="{{ list }}" wx:key="{{ item }}">
        <reap-icon type="{{ item }}" size="28" color="#80848f"></reap-icon>
        <view class="icon-item-name">{{ item }}</view>
    </view>
</i-grid>